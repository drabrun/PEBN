.navbar.navbar-default.navbar-fixed-top(role='navigation')
 .container
   .navbar-header
     button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
      span.sr-only Toggle navigation
      span.icon-bar
      span.icon-bar
      span.icon-bar
     a.navbar-brand(href='#') Project name
   .collapse.navbar-collapse
    ul.nav.navbar-nav
     | <li class="<%= currentNav == 'home' ? 'active' : ''%>"><a href='#' >Home</a></li>
     | <li class="<%= currentNav == 'about' ? 'active' : ''%>"><a href='#about' >About</a></li>
     | <li class="<%= currentNav == 'contact' ? 'active' : ''%>"><a href='#contact' >Contact</a></li>
     | <% if(loggedIn != null){ %>
     | <li class='dropdown'>
     |  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <span class="caret"></span></a>
     |  <ul class='dropdown-menu' role='menu'><li><a>User management</a></li></ul>
     | </li>
     | <% }   %>
    ul.nav.navbar-form.navbar-right
    |<% if(loggedIn == null) { %>
    |<ul class='nav navbar-form navbar-right'><button id='loginBtn' class='btn btn-primary'>Log In</button></ul>
    |<% } else { %>
    |<p class="navbar-text navbar-right">Signed in as <a href="#"  >Mark Otto</a></p>
    |<% } %>

